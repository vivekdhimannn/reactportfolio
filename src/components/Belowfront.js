import React from 'react'

export const Belowfront = (props) => {
  return (
    <div className="Belowfront">
        <div className="heading">
            <h1>{props.heading}</h1>
            <p>{props.text}</p>
        </div>
        </div>
  )
}
